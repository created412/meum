# 메움 · Meum

### 메신저 움켜쥐기
**메신저에서 놓친 선생님들의 업무를 메워드립니다.**

브리티 메신저와 GOE메신저 쪽지를 읽어 **할 일과 마감일을 뽑아내고, 확인을 거쳐
바탕화면 패널과 달력에 정리해 주는** 프로그램입니다.

- 계정 연결도, 휴대폰 연결도 필요 없습니다. 이 컴퓨터 안에서만 동작합니다.
- 하루 두 번(아침·점심) 자동 실행되며, 두 시각 모두 바꿀 수 있습니다.
- 할 일은 자동으로 사라지지 않습니다. **교사가 체크한 것만** 사라집니다.


> **교원 업무경감용 오픈소스 프로그램** · Windows 10/11 · 설치 불필요 · 서버 없음 · 무료(MIT)
> 쪽지 내용은 이 컴퓨터 밖으로 나가지 않습니다. → [개인정보 처리방침](PRIVACY.md)

## 0. 내려받기

- **실행 파일** — [Releases](../../releases) 에서 `Meum-v1.0-win64.zip` 을 받아 압축을 풀고 `Meum.exe` 실행
- **소스** — 아래 「소스로 실행하는 경우」 참고

Python 설치가 필요 없고, USB에 넣어 다녀도 동작합니다.

---

## 1. 설치

### 다른 컴퓨터에서 쓰는 경우 (권장)

`dist/Meum` 폴더를 통째로 복사해 옮기고 **`Meum.exe`를 두 번 클릭**하면 됩니다.
Python 설치가 필요 없습니다. USB에 넣어 다녀도 동작합니다.

처음 실행하면 **설치 마법사**가 세 단계를 안내합니다.

| 단계 | 내용 |
|---|---|
| 1 | 브리티 메신저 연결 확인 |
| 2 | 실행 시간 설정 (아침·점심, 각각 수정 가능) |
| 3 | 완료 · 시험 실행 |

### 소스로 실행하는 경우

```bash
pip install uiautomation pywin32
python run.py
```

(구글 캘린더 동기화까지 쓰려면 `google-api-python-client google-auth-oauthlib` 추가)

exe를 직접 만들려면 `python build.py` — 결과는 `dist/Meum/`.

## 2. 매일 동작

```
아침·점심 정해진 시각
  → 「쪽지 정리」 창: 새 쪽지 목록 → [정리해서 저장] / [나중에]
  → 「저장할 일정 확인」 창: 넣을 것 선택, 제목·마감 수정 가능
  → 바탕화면 패널과 달력에 반영
```

- 그 시각에 PC가 꺼져 있었으면 켠 뒤 **보충 실행** (`StartWhenAvailable`)
- 직전 성공 실행이 **90분 안**이면 건너뜀 (보충 실행이 겹치는 것만 방지 —
  '오늘 이미 했나'로 막으면 점심 실행이 아침 실행에 막히므로 시간 간격으로 거른다)
- 며칠 PC를 안 켜도 다음 실행 때 그 기간을 한 번에 소급

## 3. 바탕화면 패널

화면 오른쪽에 붙는 세로 판. `Meum.exe --widget`.

- **위: 작은 달력** — 오늘 강조, 일정 있는 날에 점(빨강=지남/오늘, 초록=내 일,
  보라=알아둘 일, 주황=학사일정). 날짜를 누르면 그 날만. 연·월 클릭 → 큰 달력.
- **아래: 할 일 목록** — 날짜별, 급한 순. 탭: `내 할 일`(학생 행사 등 제외) / `전체` / `쪽지별`.
- 네모(☐) 클릭 = 완료 · **두 번 클릭 = 원래 쪽지 열기** · 완료 직후 15초간 `↺ 되돌리기`.
- 제목은 압축됨 — "점검사항 확인 후 수정된 평가계획을 8/19(수) 14시까지 보내주시기
  바랍니다." → **"수정된 평가계획 제출"**. 원문은 두 번 클릭으로.

## 4. 달력 · 학사일정

자체 달력을 씁니다(계정 불필요). 큰 달력(`--calendar`)에서:

- `+ 학사일정 추가` — `2026-10-13 ~ 2026-10-16` 처럼 여러 날 일정도 가능
- `학사일정 가져오기(.ics)` — 학교 학사일정 파일을 통째로
- 구글 캘린더는 **선택** — `config.json`의 `calendar_backends`에 `"google"`을 넣고
  본인 계정을 한 번 연결하면 10분마다 동기화되고 완료 시 캘린더에서도 빠짐.
  배포용에는 켜지 않는 것을 권장.

## 5. 어떻게 읽어오는가

두 메신저 모두 로컬 DB가 **암호화**되어 있어 화면 접근성(UI Automation)으로 읽습니다.

| | 브리티 (Electron) | GOE (MFC 네이티브) |
|---|---|---|
| 목록 | `WM_GETOBJECT`로 접근성 활성 후 판독 | 직접 그리기 — 판독 불가 |
| 본문 | 상세 창에서 전문 판독 | `EditControl.Value`로 전문 판독 |
| 수집 방식 | 신규 목록 항목만 열람 | 위에서부터 열어보다 **이미 본 쪽지에서 중단** |
| 클릭 | 창에 마우스 메시지 직접 전송 (커서 안 움직임, 가려져 있어도 동작) | 동일 |

**하지 않는 것**: 답장·전달·삭제. 해당 코드가 아예 없습니다.

## 6. 명령어

```bash
Meum.exe                 # 지금 정리 실행
Meum.exe --setup         # 설치 마법사 (실행 시각 변경)
Meum.exe --widget        # 바탕화면 패널
Meum.exe --calendar      # 큰 달력
Meum.exe --list-only     # 목록만 읽기 (아무것도 바꾸지 않음)
Meum.exe --status        # 실행 이력
Meum.exe --headless      # 확인 창 없이 (확신도 높은 것만 자동 반영)
Meum.exe --uninstall     # 자동 실행 등록 해제
```

## 7. 설정 — `%LOCALAPPDATA%\Meum\config.json`

| 항목 | 기본값 | 설명 |
|---|---|---|
| `run_time` | `08:40` | 아침 실행 시각 |
| `run_time_lunch` | `12:40` | 점심 실행 시각 |
| `sources` | `["brity","goe"]` | 읽을 메신저 |
| `collect_mode` | `full` | `full`(본문 열람) / `preview`(목록만) |
| `calendar_backends` | `["ics"]` | `"google"` 추가 시 구글 동기화 |
| `auto_approve_confidence` | `0.85` | 이 이상이면 기본 체크 상태로 제시 |
| `deadline_default_time` | `17:00` | 시각 없는 마감의 기본 시각 |
| `widget_width` | `380` | 패널 폭 |

## 8. 테스트

```bash
python tests/test_dateparse.py    # 한국어 마감일 해석 12건
python tests/test_extractor.py    # 실제 쪽지 본문 추출
python tests/test_multievent.py   # 한 쪽지에 행사 여러 개
python tests/test_calendar.py     # ics 생성 · UID 고정(중복 방지)
python tests/test_titles.py       # 제목 압축 · 내 일/알아둘 일 구분
```

## 9. 파일 위치

| 무엇 | 어디 |
|---|---|
| 설정 | `%LOCALAPPDATA%\Meum\config.json` |
| 기록(쪽지·할 일·학사일정) | `%LOCALAPPDATA%\Meum\state.db` |
| 실행 로그 | `%LOCALAPPDATA%\Meum\logs\` |

기록은 컴퓨터마다 따로 보관됩니다. 배포 폴더를 옮겨도 기록은 따라가지 않으며,
새 컴퓨터에서는 최근 14일을 소급해 한 번 정리합니다.

---

## 10. 개인정보와 보안

이 프로그램은 **교사가 이미 자기 메신저에서 볼 수 있는 쪽지를, 그 교사의 컴퓨터 안에서 다시 정리해 주는** 도구입니다. 정보를 새로 모으거나 밖으로 보내지 않습니다.

- **서버가 없습니다.** 회원가입·로그인·계정 생성 절차가 없고, 기본 설정에서 네트워크 통신을 하지 않습니다.
- **저장 위치는 이 컴퓨터뿐입니다.** `%LOCALAPPDATA%\Meum\` 아래에만 기록되며, Windows 계정별 보호 영역이라 본인만 접근할 수 있습니다.
- **메신저를 바꾸지 않습니다.** 답장·전달·삭제 기능이 아예 구현되어 있지 않습니다. 쪽지를 열고 닫는 동작만 합니다.
- **전화번호는 가려집니다.** (`mask_phone_numbers`, 기본 켜짐)
- **지우면 끝입니다.** `%LOCALAPPDATA%\Meum\` 폴더를 삭제하면 모든 기록이 즉시 사라집니다. `Meum.exe --uninstall` 로 자동 실행 등록만 해제할 수도 있습니다.

외부 통신이 발생하는 두 기능은 **모두 기본 꺼짐**이며, 이용자가 설정 파일을 직접 고쳐야만 켜집니다.

| 기능 | 기본값 | 켰을 때 |
|---|---|---|
| 구글 캘린더 동기화 (`calendar_backends`에 `"google"`) | 꺼짐 (`["ics"]`) | 본인 구글 계정으로 인증 후, 본인 캘린더에 할 일 제목·마감일시가 등록됨 |
| AI 추출 (`extractor: "claude"`) | 꺼짐 (`"rule"`) | 본인 API 키를 직접 넣은 경우에 한해 쪽지 본문이 해당 API로 전송됨. **배포 실행 파일에는 필요한 구성요소가 포함되어 있지 않아 동작하지 않으며 규칙 기반으로 자동 전환됨** |

자세한 내용은 [PRIVACY.md](PRIVACY.md) 를 보세요.

## 11. 라이선스와 문의

- 라이선스: [MIT](LICENSE) — 학교·교육청에서 자유롭게 쓰고 고치고 나눠 주셔도 됩니다.
- 만든이: 이재영 (경기 양지고등학교 교사)
- 문의: histo5731@gmail.com

버그 제보와 개선 제안은 [Issues](../../issues) 로 남겨 주세요.
